# Ignore Python virtual environments
venv/
venv-linux/

# Ignore Python cache
__pycache__/
*.pyc

# Ignore local config
config.py

# Ignore deployed service manifest (may contain secrets)
service.yaml

# Ignore trained model weights (too large for git, stored on Google Drive)
models/rooftop_model.pt

# Secrets and env files
.env
.env.local
.env.*.local
*.env

# Test artifacts
math_test*.jpg
test_esb.jpg
pipeline_test_outputs/
test_addresses_local.csv
geometry (1).py

# Sensitive / local-only files (do not publish on public fork)
Existing_Market_TAM.xlsx
jobs.db
storage/
temp_uploads/
training_images/
training_addresses.csv
training_setup/
download_training_images.py
.gitattributes

# Scratch / one-off validation files (Phase 3, throwaway)
scratch_*.py
scratch_*.csv
scratch_results.json
smoke_*.py
